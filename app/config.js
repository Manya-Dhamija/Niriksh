// Niriksh Score Engine — runtime configuration.
//
// This is the ONE file to edit after deploying the backend somewhere
// (Render, Railway, Fly.io, etc.) -- no rebuild, no npm, just edit this
// file directly in your deployed app/ folder (or in GitHub's web editor)
// and refresh the page.
//
// Leave apiBaseUrl empty ("") to use http://localhost:8000, which only
// works if you are running the backend on your own machine.

window.__NIRIKSH_CONFIG__ = {
  apiBaseUrl: "https://niriksh-cf5y.onrender.com" // e.g. "https://niriksh-backend.onrender.com"
};
